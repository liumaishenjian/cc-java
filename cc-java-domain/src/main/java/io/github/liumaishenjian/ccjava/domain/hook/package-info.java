/**
 * 框架无关的 S09 Hook 生命周期协议和值对象。
 *
 * <p>本包只表达事件、Matcher、单 Handler 结果和聚合意见，不执行命令、HTTP 或
 * 任何外部副作用。Hook 结果不能取代 Agent Runtime、Permission 或 Tool Pipeline
 * 的最终权威决定。</p>
 *
 * @since 0.1.0
 */
package io.github.liumaishenjian.ccjava.domain.hook;
