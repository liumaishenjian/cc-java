# S12 Sub-Agent + Worktree 差距报告（Phase 3 整体 Review 后）

```text
Release / Commit: N/A - dirty worktree candidate; final implementation commit pending
Reference Baseline: R2026.03
Authorized Snapshot ID: AUTH-SRC-2026-07-29-A
Current Learning Stage: S12
Stage Status: IN PROGRESS（G0-G2 PASSED；G3-G6 OPEN）
```

## 当前可证明的 L1 骨架

- `delegate_agent` 作为最高 `SYSTEM_OR_DESTRUCTIVE` Effect 的普通 Tool 进入唯一 Pipeline；前台等待超时会取消并要求终态，不再隐式遗留后台任务。
- strict `*.agent` schema、User/Project 冲突隔离、immutable digest snapshot、builtin Tool 可见集交集与同一 `AgentRuntime` child scope 骨架。
- 原子多维 reservation、实际 turn/tool/估算 token/output/time settlement、journal terminal 写入失败的 fail-closed 可观察终态。
- 有界 Supervisor、后台 inspect/wait/cancel、parent/shutdown cancellation 与 no-replay journal 恢复骨架。
- `ParallelToolBatchExecutor` 的实例级并行上限、唯一 Pipeline、稳定原顺序、全在飞取消与有界收敛。
- Git Worktree identity 基于 git common-dir；真实 Git 自动测试证明 clean remove 无 registration/branch 泄漏，以及 active、identity mismatch、cross-manager 未恢复 lease、missing registration、ignored/dirty/new commit 均保守保留。创建前 ancestor symlink/reparse 检查和 Git timeout/进程树清理已由生产实现提供，但当前缺少可移植的自动故障注入证据，不作为本轮 candidate 证据。
- stdio client 的 task inspect/wait/cancel/keep/remove helper、严格 task/worktree event schema，以及 React/Ink `/task wait|cancel|keep|remove` 用户输入链路与 task card。

## Phase 3 整体修正后的能力结论

| Feature | 当前 | 证据 |
| --- | ---: | --- |
| `SUB-01..05/07..10`、`CTX-15`、`HOOK-08`、`TOOL-15` | L2 candidate | Project definition 复用 Extension/S08 精确 trust 结果；child 使用独立 Session/Permission/Registry/Workspace bootstrap；恢复终态进入 registry 但不重放；Stop additional context 只投影父下一回合；TOOL-15 已接入 AgentRuntime 原始 batch path。 |
| `SUB-06`、`HOOK-11` | L1 candidate | 保守模型子集、原子预算 reservation/actual settlement，以及宿主可信纯收窄 Hook seam；Provider pricing/discovery 与模型决策 Hook 仍延期。 |

安全矩阵覆盖 definition schema/冲突/trust/TOCTOU、host-owned depth、budget/queue/terminal CAS、parent/shutdown cancellation、journal no-replay、Tool 可见集交集、隐私 report、并行 Tool 顺序/Call ID/取消，以及 Worktree identity/dirty/ignored/new commit/registration/clean remove。生产 Composition 通过 Headless `delegate_agent → unique Pipeline → Supervisor → child AgentRuntime` 及 stdio/TUI task wait/cancel/keep/remove 路径复验；Worktree reparse、Git fault/timeout、Windows remove/branch-lock cancellation recovery 仍明确保留为自动测试 gap。

## 真实 Eval 与证据纠正

旧 `S12MultiAgentEvalTest` 的公式生成指标已经删除。新测试在六个公开 seed、每策略五次（60 个实际 child run）中真实创建 `AgentSupervisor`、`ChildRuntimeScope`、独立 `AgentSession` 和同一 `AgentRuntime`，以同步 Fake Model Gate 实测顺序/并发墙钟；两侧完成率均为 100%，冲突和未审批副作用为 0，多 Agent 墙钟中位数改善超过 ADR-062 的 20% 门槛。Token 收益不再伪造或声明。

协调者在最终工作树独立执行标准 `.\mvnw.cmd clean verify`，连续通过 838 tests/21 skips、0 failure/error（Domain 53/0、Core 238/0、Spring 45/2、Tools 162/8、MCP 13/0、CLI 327/11）；TUI 133/133、launcher 59 assertions、Dashboard 与 diff/NUL/privacy checks 也通过。工作树仍是 dirty candidate；G3-G5 可作为候选通过，G6/Stage Exit 必须等待协调者创建真实 implementation Commit 后做 commit-scoped 全复验。

## 延期边界

`SUB-11` Team Board/peer messaging、remote worker/daemon、跨重启继续执行、稳定外部 task protocol、Provider discovery/pricing、模型 Prompt/Agent Hook 决策、自动 commit/merge/push 及 S13 OS Sandbox 继续延期；Git Worktree 不是 OS Sandbox。
