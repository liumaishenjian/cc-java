# S14 Production Harness Gap Report

- Reference Baseline: R2026.03
- Authorized Snapshot: AUTH-SRC-2026-07-29-A
- Public Cross-check: Codex rust-v0.147.0 fixed tag/commit
- Stage Status: ACCEPTED with documented deviations / Stage Exit ACCEPTED
- Verified Commit: `dff814c1bb5a659979e007061e6d10a0a9ff6e82`

## 已完成

1. Provider capability/router、typed retry-after、fallback fence、usage/cost-known 与 NetworkAccess seam；真实 OpenAI-compatible text/stream/single-tool/usage E2E 通过。
2. Anthropic Spring AI Adapter protocol mock 覆盖 text、stream、single/multi-tool、usage、cancel、429 Retry-After、5xx 与 context-limit；finish reason 与 service exception 已归一化。
3. 12 个注册 seeds 各重复 5 次，合计 60 个真实 production-harness 场景；覆盖 direct final、built-in Tool 多回合、Call/Result ID、permission/tool failure 恢复、cancel、turn limit、context preparation、canonical Session create/continue/resume、SDK Tool loop 与 stable initialize/run/event/唯一 terminal/idempotency。报告从 AgentRunResult、模型收到的 ToolResult、事件和 stable envelope 聚合 completion/wall-clock/violations/modelTurns/toolCalls/stopReason；60/60 完成、0 violation，usage/cache/cost 仍 unknown，EVAL-03 保持 L2、不升 L3。
4. Session control 已改为服务端读取 canonical、逐字段脱敏、从实际 writer/recovery/migration 推导 retention 状态；Plugin global writer 覆盖 recovery、完整 install/uninstall 与真实 registry migration，create-only publish、phase/digest/restart recovery/冲突 fail-closed 已验证，CFG-10/PLUGIN-04 达 L2；OTel 有界异步出口、drop counter 与 graceful close 证据使 OBS-06 达 L2。
5. Windows launcher/process/package 与 WSL Ubuntu launcher shell/checksum contract smoke 通过；install/upgrade/current/LKG rollback 在 target 隔离目录通过。
6. first-v1 compatibility fixtures 验证 v0/v1 coexist、v1 codec 与 major negotiation boundary；release manifest/checksum/SBOM 通过。

## Commit-scoped 验证

- focused daemon/plugin/managed suites：7 tests，0 failure/error；
- 第一次 `mvnw clean verify`：历史 AgentRuntime cancellation 2 秒窗口偶发 timeout；同一用例立即隔离重跑 1/1 PASS；
- 第二次完整 `mvnw clean verify`：BUILD SUCCESS，911 tests/10 skips，全部模块 0 failure/error；
- 严格 `javadoc:aggregate`：BUILD SUCCESS，0 warning；Dashboard check/self-test PASS；
- TUI 133/133、launcher 59 assertions、release 73 Maven components/77 checksums；
- Windows+WSL launcher/package 与 install/upgrade/LKG rollback smoke 通过；
- 真实 OpenAI suite 为 1 pass/1 conditional skip；daemon 独立 OS process E2E 已覆盖 unauthorized、oversize、initialize、run event/唯一 terminal、shutdown 退出与 ownership 重取。

## Accepted Deviations / 后续 gap

1. **真实 Anthropic**：本机无 Anthropic 凭证；protocol mock 不计真实 Provider。`MODEL-07` 保持 L1，真实 text/tool/usage/cancel/error suite 后续显式运行。
2. **N/N-1 已发布 artifact**：Apache-2.0 已解除许可证门禁，但首个 tag workflow 尚未完成且无已发布 N-1；`CLI-11/DIST-04` 只到 L2，不宣称 L3。
3. **WSL Java process**：Ubuntu 发行版无 JDK 21；已验证 launcher shell contract 与全部 package checksums，未把它写成 Linux Java process PASS。
4. **平台与分发**：2026-08-16 corrective 已补齐编译 TUI、Java/Node 自包含 CI 包、固定 archive、install/update/uninstall 与 Windows 隔离安装实测；Apache-2.0 已选定，首个 tag workflow 与 Linux CI 结果仍待验证。macOS、Native Image、publisher signature/revocation、后台更新以及远程/TLS/账户/多租户 Daemon 延期。
5. **能力延期**：`TOOL-17/18`、`PLUGIN-06`、`SUB-11` 保持矩阵差距；Plugin signature 不证明 publisher identity/revocation/root rotation/transparency log。
6. **真实 L3 比较**：真实第二 Provider、已发布 compatibility 与跨平台长期重复不足，因此相关能力均按 L1/L2 诚实分级。

## 退出结论

S14 在实现 Commit `dff814c1bb5a659979e007061e6d10a0a9ff6e82` 上完成 Commit-scoped G0-G6，Stage Exit 为 Accepted with documented deviations。该接受只覆盖矩阵冻结的 L1/L2 范围：EVAL-03、PLUGIN-04、OBS-06 等已到 L2，但 CFG-07 保持 L1，SESSION-14、MODEL-07/09、CTX-16、PLUGIN-05、OBS-04 等保持 L1，不代表 Capability 整体提升。真实 Anthropic 在线证据、已发布 N-1 升级、WSL JDK21、macOS/Native Image/公开更新服务和 Marketplace/signature identity 等继续作为后续 gap；第一次 cancellation timeout 也保留为稳定性历史信号。下一步进入 S15 规划，不得把这些 gap 写成已完成或据此声明 L3。
