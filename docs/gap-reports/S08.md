# S08 Instructions + Settings 差距报告

> 状态：Accepted。ADR-048 已在 corrective implementation Commit `8fabd94b66881a4a8236cccabd4ae61dd39845d4` 上完成 G0-G6；独立最终 review 无 blocking finding。

## 已保留的历史实现

- 分层 Instructions、Settings v1/LKG、受控 Runtime 映射、显式 `/compact`、`/context`、recovery-gated `/resume`、Session steering 队列及 S05/S06/S07 安全 Gate 的既有实现与证据不因重开而被删除。
- `BOOT-06`、`CFG-09` 保持 L2，但必须在纠错实现中重新证明 Surface 诊断投影没有退化；`CLI-07`、`CTX-12/13`、`MODEL-08`、`PERM-12`、`SESSION-04/05/09` 只做回归，不提升等级。
- 历史 S07 定量 Eval、全量 Maven、TUI 与 Javadoc 结果不是新实现 Commit 的证据，必须按 ADR-048 重跑。

## 重开原因与 Corrective Blocker

| 范围 | 当前缺口 | 关闭条件 |
| --- | --- | --- |
| `CLI-08` | 可见命令选择与类型化安全结果的真实交互证据不足 | 新实现 Commit 上的 Command Catalog、严格 Projection、TTY Demo 与协议负例通过 |
| `CLI-09` | 字符串式编辑未证明 grapheme 光标、视觉多行导航、Home/End/Delete/词移动、History/Completion 优先级和 viewport | `ComposerState` 纯 Reducer 及 Unicode/Resize/键位矩阵通过 |
| Large Paste | 8,192 被错误施加到展开内容，且与 UTF-16、约 1 MiB 无损预算及 64 KiB NDJSON 单行安全边界不一致；静默截断不可接受 | 8,192 只限可见结构；短生命周期 payload + 1 MiB UTF-8/显式 code-point、UTF-16 ceiling；W4 原子 begin/chunk/commit 校验 count/bytes/SHA-256，失败无 Session/Canonical 写入或部分 Run；完整输入仍由 256K Token Context pipeline 类型化接受/拒绝/压缩 |
| Model diagnostics | 用户可见摘要无法区分 transport/decode/finish/tool JSON 阶段，又不能通过原始异常日志破坏隐私 | OFF 默认、SAFE/VERBOSE opt-in、封闭枚举、Run/turn/attempt 关联、有界轮转 JSONL、失败隔离和 sentinel 非泄漏测试通过 |
| Stage evidence | 历史 Commit 未实现 ADR-048 | 新固定 Commit 的 G3-G6、Demo/Gap、独立 review、矩阵/README/看板对账完成 |

## 当前实现与证据

- Composer/TUI、折叠无损 Paste、stdio 原子提交与 ModelDiagnostic 已完成；完整 Maven Reactor 全绿：Domain 45、Core 172、Spring 43（2 skipped）、Tools 101（8 skipped）、CLI 227（11 skipped），全部 0 failures/errors；TUI 111/111、launcher 59/59。
- Review 已修复早期 Composer/diagnostic/sink 问题，以及 W4 的转义后 NDJSON 行上限、ack/draft/paste 生命周期与正文泄漏、viewport/cursor 实际渲染、主动 assembly expiry/cancel/tombstone、统一 logical request ID；fresh review 又修复 delayed ack 覆盖新输入、重复 begin/inputId mismatch 生命周期和 soft-wrap 双 cursor。最终 CLI stdio 33/33、TUI 111/111，独立复审无 blocking finding。
- 真实 TTY 已验证 launcher + Provider `OK`（1 turn/0 tools）、`/context` 数值投影、无参数 `/compact` 当前 Session 安全拒绝、2,006-byte Paste 折叠为 `[粘贴 #1 · <64KiB]` 且正文不暴露、raw Left/Z 插入和 Shift+Enter 第二视觉行。
- corrective implementation Commit `8fabd94b66881a4a8236cccabd4ae61dd39845d4` 已固定并完成 G6 Commit-scoped 对账。

## 当前能力声明

- `CLI-08`、`CLI-09` 已恢复 L2。
- `OBS-04` 保持 L1；本地诊断平面不等于 S14 的聚合、OTel、Export 或稳定外部协议。
- G0-G6 Passed；S08 Stage Exit Accepted；不存在遗留 S08 blocker。

## 继续延期

Settings 持久写入/迁移、rules/selector 编辑、Provider discovery/多模型注册、Managed Policy、S12 Sub-Agent/后台任务/Worktree、S13 OS Sandbox 与 S14 稳定协议/OTel/Export/Retention/Migration 仍未实现。诊断本地 JSONL 不是 Session durable record、稳定机器 API、遥测导出或 OS Sandbox。

## ADR-049 Supplementary Accepted（2026-08-08）

- `CLI-13`/`CTX-19` 的显式 Workspace 文件引用已在实现 Commit `5910a8f` 完成 G3-G6：安全快照、JSONL Resume/Fork、确定性模型信封、stdio 候选和 TUI token 补全均有离线测试；ADR-050 的有界读取、跨平台精确编辑与先读证据纠正也已纳入最终审查。
- 完整 Reactor 689 项（Spring 2 skipped）、TUI 128/128、launcher 59/59 与进度看板复验通过；两项能力提升至 L2，S08 恢复 Accepted。
- 当前保持 L1，唯一 Stage 流程缺口是独立 implementation Commit 与 Commit-scoped G6/看板对账；不得提前宣称 Supplementary Accepted。
- 当前仍不支持目录附件、glob、图片/二进制附件、文件内容 watcher、跨 Workspace 引用或稳定外部附件协议；这些能力没有被 `@file` 切片暗示为可用。
- steering 明确定义为“提交时快照”：排队后文件变化不会替换已接受附件。这是可重放语义，不是后台持续跟踪文件。
