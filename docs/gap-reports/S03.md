# S03 Read Tools 差距报告

- Stage: S03 Read Tools
- Status: Accepted on implementation Commit `71a2818`
- Date: 2026-07-30
- Reference Baseline: `R2026.03`
- Authorized Snapshot ID: `N/A - Not Used`
- Decision: [ADR-032](../adr/ADR-032-s03-read-tools-security-contract.md)

## 已独立实现

- `list_files`：稳定协议路径、glob、深度/条目限制、不跟随目录链接；
- `search_text`：受控 ripgrep、字面/正则、Glob/type、大小写、多行、上下文、
  content/files/count、offset/limit、JSON 机器协议、取消、超时、一次降线程重试和
  Java 字面降级；
- `read_file`：1-based 行分页、严格 UTF-8/BOM、文件大小和 continuation；
- `git_status`：分支、staged/unstaged/untracked 安全摘要；
- `git_diff`：staged/unstaged 枚举模式、Guard pathspec、固定只读 ProcessBuilder；
- WorkspaceGuard：绝对/UNC/drive-relative/traversal、realpath containment、Symlink/Junction；
- 固定敏感路径策略与安全模板例外；
- Tool 语义 metadata 和 Pipeline 最终 Unicode 字符 ceiling；
- 根 `AGENTS.md` 单层加载及不得提权测试；
- WorkspaceSnapshot；
- stdio/TUI Tool started/completed/failed 脱敏投影；
- Markdown Assistant 正文、按模式和条目数聚合的连续 Tool 摘要，以及分层终态展示；
- Scripted Model 五 Tool E2E、安全错误后恢复和 Windows Junction 实证。

## 相比成熟参考仍缺失

| 差距 | 影响 | 后续 Stage |
| --- | --- | --- |
| list/search continuation 只有安全重试提示，不是稳定游标 | 超大结果集需要模型缩小 path/glob | S07/S14 |
| rg 只从系统 PATH 解析，没有项目内置二进制、签名和跨平台发行验证 | 新机器必须自行安装 rg | S14 |
| Git rename 的 C-style quoted path 只支持保守子集 | 极端特殊文件名可能被过滤 | S14 跨平台兼容 |
| 只有 search_text 的只读 rg 子进程接入 Run 取消；通用命令进程控制尚未实现 | 未来 Patch/Shell 仍不能复用该边界 | S04 |
| 根指令只支持 Workspace 根一层 | 子目录作用域和用户级规则缺失 | S08 |
| stdio v0 仍非稳定公共协议 | 外部 Client 不应依赖兼容性 | S14 |
| TUI 只有默认紧凑 Tool 摘要，没有展开单次 Tool 详情与历史导航 | 深度调查时仍需从回答中的文件/行号理解过程 | S08/S14 |
| 失败 Run 的终止原因尚未进入下一轮模型历史 | TUI 能准确展示失败，但模型不能可靠回答“上一轮为何失败” | S06 / SESSION-09 |
| WorkspaceGuard 不是 OS Sandbox | 不能隔离未来获批进程或用户账户权限 | S13 |
| 没有自动 Context 压缩 | 长 Session 只靠单结果硬限制 | S07 |

## 独立偏离

- S03 机制校正后由受控 ripgrep 提供显式正则；Java 降级仍只接受字面文本；
- 授权源主要使用文本结果解析；本项目独立采用 rg JSON Lines，消除 Windows 盘符、
  含冒号路径、上下文和多行结果歧义；
- 不使用通用 Shell 或第三方 grep/git 库；
- 项目指令采用通用根 `AGENTS.md` 最小约定，不复制某产品完整配置机制；
- 敏感路径策略在 S08 前固定且不能由 Prompt 放宽；
- Git 错误不返回原始 stderr，牺牲部分诊断细节以守住默认隐私边界。

## 风险

- 文件系统大小写语义、Symlink 权限和 Git 路径 quoting 在不同 OS 上仍需继续回归；
- 当前测试覆盖 Windows NTFS Junction，本轮尚无独立 Linux CI 结果；
- 真实 Provider 是否稳定选择全部五个 Tool 属于模型/Provider 兼容性，不由离线 E2E 自动证明；
- Tool 全部成功不代表 Run 完成；TUI 已展示 Java 权威终态，但 S06 前不声明失败恢复语义；
- Markdown 与 Tool 聚合是展示投影，不得被当作 Tool 执行成功或 Run 完成证据；
- 固定敏感文件名不可能穷举所有秘密，用户仍不应在含未知秘密的不可信仓库中盲目运行。

## 退出判断

只有以下条件都满足后才将 S03 标记 Accepted：

1. Maven 全量测试、聚合 Javadoc、TUI check 通过；
2. Windows Junction、五 Tool Scripted E2E 和注入负例通过；
3. 真实 Provider opt-in Demo 已运行，或因执行环境拒绝而明确记录为非阻塞未验证项；
4. 矩阵、README、PRD、技术设计、Evidence、Demo、Gap 和看板完成对账；
5. Commit-scoped 复验通过。
