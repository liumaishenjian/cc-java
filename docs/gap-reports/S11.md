# S11 Skills + Plugins 差距报告（Accepted）

```text
Release / Commit: 71278431dd1e5c7c4e279b44f43e084755502a5d
Reference Baseline: R2026.03
Authorized Snapshot ID: AUTH-SRC-2026-07-29-A
Public Source Snapshot: OpenAI Codex rust-v0.147.0 / be6e8eac029b183056b7e4402879f15d2c85f61b
Current Learning Stage: S11
Stage Status: ACCEPTED（G0-G6 PASSED；Stage Exit Accepted）
Environment: Windows 10 / Java 21 / Maven Wrapper 3.9.16 / Node 22
```

## 已验收的 G3 实现切片

- Skill metadata-first catalog、本地 lazy 正文/资源、Plugin Session-frozen 正文/资源、显式/模型双入口、transient Projection、allowed-tools 交集与 Pipeline 内 execute=0 Gate；
- privacy-safe `skill.invoked` / `skill.completed` JSONL，精确 manifest/body/content/resources/effective-tools/hooks/Plugin/MCP identity 恢复校验，Resume no replay 与 compaction 每回合从 Run state 重建 Projection；
- Plugin strict manifest/namespace、immutable snapshot、fingerprint trust、host-side Provider SPI、真实 MCP-backed Plugin Tool、staged/quiescing lifecycle 与 Run generation lease；
- Skill Hook 在成功 Projection 后绑定独立 lease，覆盖失败/取消、多 Skill、compaction、唯一终态逆序 exactly-once cleanup 与 Resume/Fork no restore；
- 普通 Headless production composition E2E 覆盖 Plugin Skill/Tool/Hook、Fake MCP deny/allow、Call ID、当前 Session no drift、新 Session distrust mutation；
- Java print/stdio、React/Ink TUI 的类型化 Skill command 和 privacy-safe lifecycle 观察协议；
- 历史 G4 工作树基线为 806 tests（24 skips）、CLI 311 tests（12 skips），focused G4 矩阵 93 tests 零失败/跳过；该计数已被最终独立验收取代。协调者最新 `clean verify` 精确汇总为 813 tests（21 skips）：Domain 53/0、Core 226/0、Spring 45/2、Tools 158/8、MCP 13/0、CLI 318/11，0 failure/error；TUI 129 tests、dashboard 三命令及 diff check 通过。

## G4 Commit-scoped 独立验证结果

- 20 个独立 Skill、每个完整文件 8,192 bytes：真实 eager baseline `163,840 bytes`，metadata materialization `800 bytes`（frontmatter 800 + body 0），按总量比计算降低 `99.51%`；identity digest 流式读取仍为完整 `163,840 bytes`，不把 hashing I/O 伪称为未读取。
- active Run compaction/rebuild seam 连续两次重建时，canonical 首消息、Skill Projection 顺序、effective Tool scope 与 Hook lease 保持不变，没有重复 activation/bind；normal/failure/cancel/recovery 关键路径确定性矩阵通过率 100%。
- traversal/absolute/link-reparse/oversize/invalid UTF-8/metadata 错误、Hook trust/cross-Plugin、Plugin/MCP identity drift、全部 staged install fault、quiescing/new Session distrust 均有负例；bypass、protocol orphan、orphan staging、lease/resource/Hook leak、privacy sentinel leak 均为 0。

## G5/G6 Commit-scoped 结果与仍未完成

- 可复现离线 Demo 最新实际运行 67/67 tests（Core 4、MCP 5、CLI 58），0 skip/failure/error，覆盖 metadata、双入口、Projection、Tool/Pipeline、Hook、compaction/recovery、install/quiescing、新 Session trust 与 privacy 正负观察；早期 pre-review 60/60 已被取代。
- G6 已对账矩阵、README、PRD、技术设计、ADR-058～060 引用、Evidence、Demo、Gap 与 Dashboard。
- 实现 Commit `7127843` 上的 G3-G6 与 Stage Exit 已 Accepted；下一路线节点为 S12 G0，但尚未启动。S12 Sub-Agent、后台任务与 Worktree 仍未实现。

## Capability 验收结果

`SKILL-01..07`、`CTX-14` 与 `PLUGIN-01..03` 按实现 Commit `7127843` 的 G3-G5 确定性证据达到 L2，`PLUGIN-04` 按 staged/quiescing S11 范围达到 L1。`PLUGIN-05/06`、`SEC-11`、`MCP-08`、`TOOL-16` 保持 L0；不声称 Marketplace、签名/供应链隔离、OAuth、Lazy MCP、Resource/Prompt 自动投影或 OS Sandbox。

## 参考仍更强之处

1. 成熟实现已有更完整的 Skill/plugin 动态贡献装载和长期运行经验；cc-java 已在实现 Commit `7127843` 上完成独立 G3 production composition、99.51% metadata 量化、G5 Demo 与 Commit-scoped 验收，但跨平台及长期运行经验仍有限。
2. 参考生态包含更丰富的远程发现、市场、更新、签名/治理或多运行时组件；S11 明确不实现这些。
3. 参考实现的选择质量、跨压缩恢复、插件安装并发和跨平台故障上界尚未形成 cc-java 对照数据。

## cc-java 的独立偏离

1. `allowed-tools` 固定为 runtime visibility 与 Skill allowlist 的纯收窄交集，Permission/Grant 不静态化，每个调用重新评估。
2. 禁止 nested/reentrant Skill；单 Run 每个不同 Skill 至多一次，Scope/Hook 只从正文成功投影持续到唯一终态，Resume 无活动 Run 不恢复。
3. Plugin Tool Provider 只允许宿主预注册 factory并返回有 lease/close 的 Contribution；manifest 不构造 ToolDefinition/ToolSource，S11 明确拒绝任意 JAR/Class/ServiceLoader/native/script。
4. Plugin 以 immutable Session snapshot、目录-only 原子 staged install 和 quiescing uninstall 避免运行中语义漂移。
5. Session journal 只持久化 Skill digest/调用事实，正文/资源保持短生命周期 Projection。

## 延期能力

| 能力 | 当前 | 后续 |
| --- | ---: | --- |
| `PLUGIN-04` 稳健安装/卸载 | L1 Accepted | S14 L2：崩溃恢复、迁移、跨平台管理 |
| `PLUGIN-05` Trust/Signature | L0 | S13/S14 |
| `PLUGIN-06` Marketplace | L0 | S14/S15 |
| `SEC-11` Plugin Supply Chain | L0 | S13/S14 |
| `MCP-08` Lazy Tool | L0 | 独立后续切片 |
| `TOOL-16` Tool Search/Lazy Schema | L0 | S11 后独立规模 Eval |
| forked Skill/Sub-Agent | L0 | S12 |
| OS Sandbox | L0 | S13 |

## Stage Exit 与后续路线

S11 blocker 为 0，G0-G6 与 Stage Exit Accepted。下一路线节点为 S12 G0 授权机制研究，但尚未启动；`SUB-01..10`、`CTX-15`、`HOOK-08/11`、`TOOL-15` 均保持 L0。上述延期能力以及 S13/S14 差距不因 S11 Accepted 自动升级。
