# S09 Hooks Stage 证据（2026-08-09）

- Stage: S09 Hooks
- Status: Accepted
- Release / Commit: `e4526f474fbfb597be1439bd5acb956cfcc9eafa`
- Reference Behavior Baseline: R2026.03 `REF-07`
- Authorized Snapshot ID: `AUTH-SRC-2026-07-29-A`
- Features: `HOOK-02`～`HOOK-07`、`HOOK-09`、`HOOK-10`、`HOOK-12`、`HOOK-13`
- Levels: 主生命周期/Command/Trust/Compact `L1 → L2`；loopback HTTP `L0 → L1`

## G0-G2

ADR-051 记录受控研究；ADR-052～054 固定协议、Command 与 Trust 第一切片；ADR-055 收口生产
配置、glob、Compact、HTTP、Context Projection 和资源所有权。只采纳职责、状态与验证方法，
不复制授权快照表达。远程 HTTP、模型 Hook、Sub-Agent Hook、OS Sandbox 和稳定外部协议延期。

## G3

- 固定 user/project 扩展配置、严格 JSON、精确 SHA-256 project trust 与 provider-free CLI 状态/批准；
- Command Handler 固定 argv/cwd/空环境、异步 stdin、墙钟 timeout、输出上限和进程树清理；
- HTTP Handler 每次解析 loopback、禁止重定向/Header、限制响应正文并传播取消；
- OBSERVE_ONLY 不改变控制流；Pre/Post Tool、Session/Run/Prompt/Permission 与 Pre/Post Compact 接入；
- Post Tool additional context 仅投影到下一 Model Request，不污染 Canonical Transcript。

## G4

```text
.\mvnw.cmd -pl cc-java-core,cc-java-cli -am test -DskipTests=false
.\mvnw.cmd clean verify
npm --prefix cc-java-tui run check
java scripts/ProgressDashboard.java --check
java scripts/ProgressDashboard.java --self-test
git diff --check
```

2026-08-09 相关模块复验：Domain 52、MCP 5、CLI 274（11 skipped），Core/Tools/Model 全部通过，
0 failures/errors。`S09CompactHookE2ETest` 以真实 HTTP Handler 证明 Pre Compact 阻断时摘要器不执行；
Command/HTTP/Core 测试覆盖稳定顺序、glob、阻断/观察、超时、取消、无效 JSON、Trust 失效与资源清理。
完整 `clean verify`、TUI 128/128、零警告 JavaDoc 与看板均已通过并写入 progress state。

## G5

可复现流程见 [S09 Hooks Demo](../demos/S09-hooks-core.md)：显式批准 project 配置后重启 Session，
验证 Command/loopback HTTP；修改配置后再次启动应显示 `PROJECT_TRUST_REQUIRED` 且不启动 Handler。
负例由 `S09CompactHookE2ETest`、`HttpHookHandlerTest` 与 `ExtensionConfigurationLoaderTest` 自动执行。

## G6

矩阵、README、PRD、技术设计、ADR-055、[S09 Gap](../gap-reports/S09.md)与看板同变更对账。
S09 Accepted 只表示本 Stage 的本地生命周期扩展可用，不表示远程 Hook、OS Sandbox 或 S14 稳定协议可用。
