# S10 MCP Stage 证据（2026-08-09）

- Stage: S10 MCP
- Status: Accepted
- Release / Commit: `e4526f474fbfb597be1439bd5acb956cfcc9eafa`
- Reference Behavior Baseline: R2026.03 `REF-03`
- Authorized Snapshot ID: `AUTH-SRC-2026-07-29-A`
- Features: `MCP-01`～`MCP-11`
- Levels: `MCP-01`～`07` `L0 → L2`；`MCP-09`～`11` `L0 → L1`；`MCP-08` 保持 L0

## G0-G2

ADR-056 记录授权快照与公开 MCP/Java SDK 机制研究，ADR-057 固定独立模块、Transport、权限、
恢复和差距。授权源码不进入依赖或 Fixture；官方 MCP Java SDK 2.0.0 是公开协议 Adapter 依赖。

## G3

- `cc-java-mcp` 隔离 SDK/Reactor；Core 只看到 `AgentTool`；
- 绝对 executable、结构化 argv、最小环境 STDIO，以及 HTTPS/loopback Streamable HTTP + 环境 Bearer；
- 多 Server 有界并行 initialize、稳定顺序、单 Server 失败隔离、deny-first filter 与 Server 前缀；
- MCP Network Effect 进入默认 ASK，其他来源 Network 和全部 System Effect 仍 Hard Denial；
- 首次调用失败后只重建 initialize 并重试一次；输出/分页/总量有界，二进制不进入文本；
- Resource/Prompt 元数据发现独立失败；project Server 未信任时不创建 Transport。

## G4

```text
.\mvnw.cmd -pl cc-java-mcp,cc-java-cli -am test -DskipTests=false
.\mvnw.cmd clean verify
npm --prefix cc-java-tui run check
java scripts/ProgressDashboard.java --check
java scripts/ProgressDashboard.java --self-test
```

`OfficialMcpStdioE2ETest` 启动真实 Java 子进程并执行 initialize/listTools/listResources/listPrompts/
call/close；`OfficialMcpHttpE2ETest` 使用真实 JDK loopback HTTP Server 验证 Streamable HTTP 与 Bearer；
`McpClientManagerTest` 覆盖过滤、前缀、多 Server 失败、未信任、重复配置、相对命令拒绝和单次恢复；
`S10McpHeadlessE2ETest` 验证配置到真实 STDIO Server、模型 Tool Call、Allow Once、统一 Pipeline、
匹配 Call ID Tool Result 与下一模型回合的完整路径。相关 Maven 回归 0 failures/errors。

## G5

可复现步骤和正负例见 [S10 MCP Demo](../demos/S10-mcp.md)。默认配置下 MCP Tool 必须请求审批；
拒绝审批时 Server 调用不执行。项目配置未批准或摘要变化时不创建 Transport。

## G6

矩阵、README、PRD、技术设计、ADR-056/057、[S10 Gap](../gap-reports/S10.md)与看板同变更对账。
Resource/Prompt 自动投影、OAuth、Lazy Tool Loading、稳定机器协议和 OS 网络隔离仍明确延期。
