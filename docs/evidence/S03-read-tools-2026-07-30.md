# S03 Read Tools 验证证据

## 元数据

```text
Stage: S03 Read Tools
Status: Exit Candidate
Release / Commit: WORKTREE（最终 Commit-scoped 复验后更新）
Reference Behavior Baseline: R2026.03
Authorized Snapshot ID: N/A - Not Used
Feature IDs: BOOT-02, BOOT-04, CLI-04, TOOL-04/05/06/07/12/13,
             SEC-01/02/03/10, CTX-02/05
Owner: project maintainer
Date: 2026-07-30
```

## G0：来源与授权

- 公开输入：`REF-02`、`REF-04`、`REF-05`；
- 一手技术输入：JDK 21 NIO/Charset/ProcessBuilder 与 Git 命令规范；
- 实现和测试输入：ADR-032、本项目安全规则和独立临时 Fixture；
- 授权参考源码未使用，Snapshot ID 为 `N/A - Not Used`；
- 未复制参考函数体、Prompt、错误文案、私有类型名、布局、常量或 Fixture。

## G1/G2：范围与设计

[ADR-032](../adr/ADR-032-s03-read-tools-security-contract.md)固定 15 项 Feature、退出等级、五个
Tool JSON Schema、WorkspaceGuard、敏感路径、预算、Git 只读参数、根 `AGENTS.md`、安全事件
投影、延期项和可证伪场景。

## G3：实现

实现位于：

- Domain：`ToolResultMetadata`、`ToolResultTruncationReason`、S03 `ToolErrorCode`；
- Core：`ToolExecutionOutcome` metadata 与 `ToolExecutionPipeline` 最终 hard ceiling；
- tools-local：WorkspaceGuard、五个 Tool、GitReadClient、RootInstructionLoader、Snapshot；
- CLI：Headless 注册五个 Tool、只读 Permission、指令注入、stdio Tool 事件；
- TUI：协议类型、Reducer 与安全 Tool 状态展示。

无写文件、Patch、通用 Shell、Git 写操作或 OS Sandbox 实现。

## G4：专项结果

### 已执行结果

| 范围 | 结果 |
| --- | --- |
| Pipeline 字符上限/Unicode/语义 metadata | 4/4 通过 |
| tools-local 当前完整测试 | 18/18 通过，0 skip |
| Windows NTFS Junction | 1/1 通过，内部允许、外部拒绝、外部文件保留 |
| Headless 五 Tool E2E | 通过；5 Tool、6 Model Turn、唯一 COMPLETED |
| 根 AGENTS.md / 注入不得提权 | 通过 |
| stdio 安全 Tool 事件 | 通过；无参数、正文、Prompt 或路径泄漏 |
| TUI build/test | TypeScript build 通过，22/22 |
| Maven clean verify | 通过（最终文档变更后将再次执行） |

已覆盖：绝对/UNC/drive-relative/traversal、内部/外部链接、`.git`、Provider 本地配置、`.env`、
安全模板、UTF-8/BOM/非法编码/二进制/大文件、list/search/read 预算、Git staged/unstaged/
untracked/non-repo、Pipeline Unicode ceiling 和伪系统指令。

### 标准验证命令

```powershell
.\mvnw.cmd -pl cc-java-tools-local -am test
.\mvnw.cmd -pl cc-java-core -am test
.\mvnw.cmd -pl cc-java-cli -am test
.\mvnw.cmd clean verify
.\mvnw.cmd "-DskipTests" javadoc:aggregate
npm.cmd --prefix cc-java-tui run check
java scripts/ProgressDashboard.java --check
java scripts/ProgressDashboard.java --self-test
git diff --check
```

## G5：Demo

离线五 Tool E2E 与安全负例见 [S03 Read Tools Demo](../demos/S03-read-tools.md)。真实 Provider
opt-in 命令已尝试由当前执行环境启动，但 Claude Code auto-mode classifier 拒绝了真实 CLI
进程调用；这不是产品运行失败。网络/API Key 不作为普通 CI 或 S03 L2 退出前提，本轮不把
离线 E2E 冒充真实网络结果，维护者仍可按 Demo 中的命令手工复验。

## G6：对账

矩阵等级、README、PRD、技术设计、Gap、`progress-state.properties` 和生成的
`progress.html` 已同步。最终 Commit 创建后将把本文件的 Commit 字段更新为实际哈希并运行
Commit-scoped Maven/TUI/Javadoc/看板复验；在此之前 S03 保持 Exit Candidate。
