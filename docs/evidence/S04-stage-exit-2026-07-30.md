# S04 Write + Command Stage Exit 证据

## 1. 结论

- Stage：`S04 Write + Command`
- 状态：`Accepted`
- 被测实现 Commit：`16b47671a423fbb711eab298c9654de9a1fd6665`
- 参考基线：`R2026.03`
- 授权参考快照：`AUTH-SRC-2026-07-29-A`
- 证据分类：`COMMIT_VERIFIED`
- 验证日期：`2026-07-30`

S04 已完成 Approval、精确 Patch、仅创建新文件、前台命令和公开 Fixture Coding Loop。
被测 Commit 上的 G0-G6、全量 Java/TUI 测试、聚合 Javadoc 与进度看板均通过，因此
Stage Exit 接受。此次退出对账不再提升 Capability Level。

## 2. Commit 边界

生产实现最初提交为 `cd5a8b3`。在该 Commit 的全量复验中，业务断言已经通过，但
Windows 上测试清理临时 Git pack 文件时出现短暂文件占用。`16b4767` 为测试基础设施
增加有界重试并同步证据和看板；以下结果全部在 `16b4767` 上重新取得。

只有 `16b47671a423fbb711eab298c9654de9a1fd6665` 被本证据包接受，不能把此前的
Worktree 结果或 `cd5a8b3` 单独视为 S04 Stage Exit。

## 3. G0-G6

| Gate | 结果 | 证据摘要 |
| --- | --- | --- |
| G0 来源与边界 | Passed | R2026.03、REF-02/04 与 AUTH-01 已登记；ADR-035 记录受控研究和非复制边界 |
| G1 范围与契约 | Passed | Approval、Patch/Write、Command 的 Effect、审批、路径、进程、输出和取消契约已固定 |
| G2 架构与安全 | Passed | Java Runtime 保持权威；Tool 统一经过 Pipeline；取消、关闭和非交互审批 Fail Closed |
| G3 实现 | Passed | Approval、文件写入、命令执行、Lifecycle/stdio/TUI 事件和 Windows 进程树清理均已实现 |
| G4 自动验证 | Passed | Maven 169 项、TUI 38 项、聚合 Javadoc、看板检查与自测通过 |
| G5 Demo/Eval | Passed | Calculator.divide Fixture 经历越权拒绝、错误修复、测试失败、再次修复、测试成功和 Git Diff |
| G6 对账 | Passed | 矩阵、README、AGENTS、ADR、Demo、Evidence、Gap 和进度看板与被测 Commit 一致 |

## 4. Commit-scoped 验证结果

在仓库根目录、检出 `16b47671a423fbb711eab298c9654de9a1fd6665` 的状态下执行：

```text
.\mvnw.cmd verify
```

结果：成功；Java 测试共 169 项，166 项通过，3 项按设计跳过。其中 2 项需要显式启用
真实 Provider，1 项依赖当前 Windows 环境可创建 Symlink。

```text
npm.cmd --prefix cc-java-tui run check
```

结果：成功；7 个测试文件、38 项测试全部通过。

```text
.\mvnw.cmd javadoc:aggregate
java scripts/ProgressDashboard.java --check
java scripts/ProgressDashboard.java --self-test
```

结果：全部成功。聚合 Javadoc 可生成；进度页面与矩阵、状态和代码摘要一致；生成器
自测通过。

## 5. 已验证行为

- DEFAULT/PLAN 模式下 Read/Write/Process Effect 的确定性决策；
- Allow Once/Deny、审批 ID 匹配、取消和连接关闭时 Fail Closed；
- `apply_patch` 精确旧内容匹配、冲突重检、同目录原子替换和有界摘要；
- `write_file` 只创建新文件，不覆盖已有文件；
- Workspace realpath、敏感路径、Junction/Symlink 与脏工作区保护；
- 固定平台 Shell、固定 Workspace、最小环境和准确命令预览；
- stdout/stderr 有界事件、退出码、timeout/cancel 和 Windows 后代进程清理；
- 模型根据真实失败测试结果继续修复，并由 Git Diff 证明只改目标文件。

## 6. 剩余差距与下一阶段

S04 只提供固定 DEFAULT/PLAN 与 Allow Once/Deny，不包含规则来源、优先级、Session
Allow、Hard Denial 或完整拒绝恢复。下一步是建立 S05 Permission Pipeline 启动 Gate：
先对授权参考快照进行受控机制研究，形成独立 ADR、可证伪测试和能力目标，再开始生产
实现。Checkpoint、Context 压缩、后台执行、Worktree 和 OS Sandbox 仍分别留在后续
Stage。
