# S11 G5 + G6 Commit-scoped 验收结果

```text
Stage: S11 Skills + Plugins
Status: ACCEPTED
Commit: 71278431dd1e5c7c4e279b44f43e084755502a5d
Initial evidence: 2026-08-09 / final independent rerun: 2026-08-10
Environment: Windows 10 Pro 10.0.19045 / Java 21 / Maven Wrapper 3.9.16 / Node.js 22
```

## G5 可复现 Demo

使用临时、公开、独立 Fixture，通过真实 `HeadlessRuntimeSession`、`ToolExecutionPipeline`、Plugin registry/trust、Fake MCP client 和 Session JSONL 执行：

```powershell
.\mvnw.cmd -pl cc-java-cli -am `
  "-Dtest=FileSkillRepositoryTest,S11PluginSkillHeadlessE2ETest,SkillRunCoordinatorTest,PluginLocalAdapterTest,McpBackedPluginToolProviderFactoryTest,PluginMcpPipelineIntegrationTest,FileSessionStoreTest" `
  "-Dsurefire.failIfNoSpecifiedTests=false" test
```

最新结果：Core `4/4`、MCP `5/5`、CLI `58/58`，合计 `67/67`，0 failure/error/skip，BUILD SUCCESS。早期 pre-review 运行曾为 `60/60`（CLI `51/51`），已由新增 7 个 `PluginLocalAdapter` 回归后的本次结果取代，不作为最终分母。完整场景、正负观察和分母见 [`../demos/S11-skills-plugins.md`](../demos/S11-skills-plugins.md)。

## G6 Commit-scoped 对账

| Feature | 验收等级 | Commit-scoped 证据 |
| --- | ---: | --- |
| `SKILL-01..07` | L2 | metadata 99.51% 降幅、双入口、Projection、resource、Tool scope、Hook、recovery/no replay |
| `CTX-14` | L2 | catalog 启动不 materialize 正文；正文/资源仅在调用时进入 transient Projection，canonical transcript 不持久化 |
| `PLUGIN-01..03` | L2 | strict manifest/namespace/snapshot、Skill/Hook/MCP bundle、宿主 MCP-backed Provider、统一 Pipeline 与 lease/close |
| `PLUGIN-04` | L1 | directory-only staged install、全 fault point Fail Closed、quiescing uninstall |
| `PLUGIN-05/06` | L0 | 签名/Marketplace 延期 |
| `SEC-11` | L0 | fingerprint 不等于供应链签名或隔离 |
| `MCP-08` | L0 | 未实现 Lazy Tool Loading |
| `TOOL-16` | L0 | 未实现通用 Tool Search/Lazy Schema |

已在实现 Commit `7127843` 上同步 feature matrix、README、PRD、technical design、ADR-058～060 引用、主 Evidence、Demo、Gap 和 progress-state；G6 明确包含 `CTX-14` L2。不得将本次验收描述成 Marketplace、Signature、OAuth、MCP Resource/Prompt 自动投影、Lazy MCP Tool、OS Sandbox 或稳定迁移能力。

## 最终工作树复验

- 最新 Demo focused：Core `4/4`、MCP `5/5`、CLI `58/58`，合计 `67/67`，0 failure/error/skip。早期 pre-review `60/60`（CLI `51/51`）已被取代。
- 最终独立 Reactor：协调者运行 `.\mvnw.cmd clean verify` 成功；Surefire XML 精确汇总 Domain `53/0`、Core `226/0`、Spring `45/2`、Tools `158/8`、MCP `13/0`、CLI `318/11`，合计 `813 tests / 21 skips`，0 failures/errors，`BUILD SUCCESS`。历史 G4 工作树复验 `806/24`（CLI `311/12`）保留为时点记录，但不再代表最终结果。
- TUI：`npm --prefix cc-java-tui run check`，10 files / `129/129` tests 通过。
- Launcher：`pwsh -NoLogo -NoProfile -File .\scripts\TestCodejDevLauncher.ps1`，`59/59` assertions 通过。
- Dashboard：generate、`--check`、`--self-test` 均通过；`git diff --check` 通过，仅有 LF/CRLF 工作区提示。
- Privacy/secret scan：变更范围未命中真实凭证模式，privacy sentinel 只存在于测试断言；本机 `config/provider.local.properties` 含私密配置但由 `.gitignore` 明确忽略且 `git ls-files` 为空，不进入变更、证据或报告。

## Review 整批修正复验

- Registry：`registry.v1` 现在严格限制 64 KiB/256 entries，稳定按 ID 排序；安装保留无关 Plugin、同 ID 原子替换，卸载原子删除目标条目；物理目录删除失败且目录仍完整时用保留 backup 恢复旧索引，目录已删除后的 failure/cleanup 不恢复悬空旧条目而保留新索引并 tombstone/orphan 收口；两 Plugin install/update/uninstall/reload、delete/cleanup fault 与 `foo`/`foo-extra` 精确 content-addressed 目录共存均有确定性测试。
- Plugin Skill：正文与资源均使用 malformed/unmappable `REPORT` 的严格 UTF-8，正文沿用 128 KiB/4,000 行边界，资源继续执行 containment/NOFOLLOW/单项与总量上限；invalid UTF-8、4,001 行、非法资源 UTF-8、路径逃逸负例通过。
- NUL：变更的 130 个文件逐 byte 扫描真实 NUL 为 0；`PluginComponentDescriptor` 使用 code point 检查，TUI 测试使用 `\\0` 转义表达。
- 历史整批 Review focused 曾为 Core `4`、MCP `5`、CLI `55`，合计 `64/64`；新增 3 个卸载故障回归及其余收口后，已由上方最终 Demo `67/67` 取代。
- 历史整批 Review full 曾为 `810 tests / 24 skips`（Tools `158/10`、CLI `315/12`）；已由上方协调者最终独立 `clean verify` 的 `813 tests / 21 skips`（Tools `158/8`、CLI `318/11`）取代。两次均为 0 failures/errors、`BUILD SUCCESS`。
- TUI `129/129`、launcher `59/59`、Dashboard generate/check/self-test、`git diff --check` 与 `git diff --text --check` 均通过；privacy scan 只命中测试 sentinel 和被 Git 明确忽略的本机 provider 文件，后者未跟踪、未进入 Diff。
- G6 对账补齐 `CTX-14` L2；矩阵为 `110 L2 / 36 L1 / 51 L0`，覆盖率 `43.32%`。

## Stage Exit 与下一路线节点

实现 Commit `71278431dd1e5c7c4e279b44f43e084755502a5d` 已完成 Commit-scoped 独立复验；G3-G6 与 S11 Stage Exit Accepted，blocker 为 0。下一路线节点仅为 S12 G0 授权机制研究，但尚未启动；`SUB-01..10`、`CTX-15`、`HOOK-08/11`、`TOOL-15` 仍为 L0。
